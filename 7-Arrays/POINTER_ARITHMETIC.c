#include <stdio.h>
int main()
{
    // int i = 34;
    // int *ptr = &i;

    // printf("The value of ptr is %u\n", ptr);

    // // ptr++;

    // // ptr = ptr + 1;
    // // ptr = ptr + 2;

    // printf("The value of ptr is %u\n", ptr);

    ///////////////////////////////////////////////////////////

    // char i = '3';
    // char *ptr = &i;

    // printf("The value of ptr is %u\n", ptr);

    // ptr++;

    // // ptr = ptr + 1;
    // // ptr = ptr + 2;

    // printf("The value of ptr is %u\n", ptr);

    ///////////////////////////////////////////////////////////

    float i = 3.2;
    float *ptr = &i;

    printf("The value of ptr is %u\n", ptr);

    ptr++;

    // ptr = ptr + 1;
    // ptr = ptr + 2;

    printf("The value of ptr is %u\n", ptr);

    return 0;
}