#include <stdio.h>
int main()
{
    int age;
    printf("Enter Your Age\n");
    scanf("%d", &age);
    if (age >= 90)
    {
        printf("You are above 90, you cannot drive\n");
    }
    else
    {
        printf("You can Drive!\n");
    }

    if (age = 50)
    // here it assigns age to be 50
    // if assigned number is not ZERO then it will run to YES.!
    {
        printf("Half Century\n");
    }

    return 0;
}